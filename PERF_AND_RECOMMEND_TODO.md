# PERF_AND_RECOMMEND_TODO — 個股績效回測區 ＋ 個股推薦區（待辦）

> **狀態：待辦，尚未開工**（Ball 2026-08-16：專案二不急，先列為待辦）。
> 本檔記錄**已定案的決策與已查證的技術前提**，開工時拆成兩份獨立 SPEC 再動手。
> 兩個功能**共用同一套報酬計算**，必須先寫共用模組再接兩邊，不得各寫一份。

---

## A. 個股績效回測區

**位置**：多空自選那排的新分頁，裡面放一顆按鈕，點下去輸入**股號或股名**＋**起始日期**。

**輸出**：該日期之後的 **20／60／120 日報酬**＋**至今報酬**。
資料不足（日期太新）時，只顯示算得出的窗口，其餘標「尚未到期，還差 N 個交易日」，**至今報酬一律顯示**。

### 技術前提（已查證）

- ✅ **不需要新後端**。`INDEX_PROXY = https://stockweb-proxy.ch41083s.workers.dev`（`app.js:2055`）的
  `/yahoo/{symbol}` 路由把 `url.search` **原樣透傳**給 Yahoo chart API（`worker/index-proxy.js:104-110`），
  所以 `range` 或 `period1`/`period2` 都能用。建議用 `period1`/`period2`（Unix 秒）精準取區間。
- ✅ **上市／上櫃 fallback 有現成先例**：`app.js:313-314` 先試 `.TW`，失敗改 `.TWO`。沿用。
- 🔴 **必須用 adjclose，不能用 close**。Yahoo v8 chart 同時回 `indicators.quote[0].close`（原始價）
  與 `indicators.adjclose[0].adjclose`（還原價）。**全站目前沒有任何一處用 adjclose**（grep 確認）——
  因為既有功能都只看短期漲跌，除權息影響不明顯。但本功能算的是 60/120 日與跨年報酬，
  **台股除權息動輒 3~5%，用原始價會系統性低估報酬，且在除權息日產生假跌**。這是正確性紅線。
  開工第一件事＝實測 proxy 回傳確實含 `adjclose`。
- ⚠ **股名輸入需要一份全市場對照表**。`stocks.json` 只有本週選股清單，Yahoo/TWSE 的 API 只能
  由股號查名、**無法反查**。需產生 `stock_names.json`（股號↔股名，全市場約 1,900 檔、約 40KB），
  來源＝`stockematool/cache/stock_list.csv`，由本機腳本或 Actions 產出後 commit。
  → 這是前置工作項，開工時要先做。

### 計算口徑（待定案，開工前先確認）

- **進場日**＝輸入日期當天或之後的**第一個交易日**。
- **進場價**＝該交易日**收盤**（使用者心智是「那天收盤看到的價格」）。UI 須標明。
- **20/60/120 ＝交易日**，不是日曆日（與 stockematool 回測同口徑）。
- **至今報酬**＝最新收盤 ÷ 進場價 − 1。
- 每個窗口須標出**起訖日期與實際交易日數**，讓使用者能核對。

---

## B. 個股推薦區

**Ball 已定案的四件事（2026-08-16）**：

1. **開放給真的其他人**，不是只有自己跨裝置。
2. **新增第 4 個分頁「大家推薦」**（方案乙）。多／空只是每筆推薦的**分類標籤**，
   顯示在卡片上。**既有多頭／空頭／自選三欄完全不動**——不污染選股系統的產出，
   也不會被每週 `run_weekly` 覆蓋。
3. **卡片顯示與多空清單相同**（日／週漲幅那些小按鈕），直接 reuse 既有卡片渲染。
4. **點開詳細面板後，多顯示「推薦日以後的報酬」**：20／60／120 日＋至今 ← 呼叫 A 的共用模組。

**另記錄**：每筆推薦須記錄**被推薦的日期**（由伺服器時間決定，不由前端傳，避免造假）。

### 儲存：Cloudflare Worker ＋ KV（Ball 已同意）

- 在既有的 `worker/index-proxy.js` 加 `GET /rec`（讀清單）與 `POST /rec`（新增），綁一個 KV namespace。
- 免費額度每日 1,000 次寫、10 萬次讀，遠超實際用量。
- Ball 須在 Cloudflare 後台建 KV → Worker 設定綁定 → 重新 Deploy（約 10 分鐘，同當初部署流程）。
- CORS 那套現成程式碼直接沿用。

### 防濫用（我的預設設計，Ball 可否決）

因為是**公開可寫**端點，被掃到就會有垃圾。預設採「開放但有限制」而非密碼制
（密碼制與「真的大家」的意圖衝突）：

- Worker 端 **IP 每日寫入上限**（如 10 筆）。
- **股號必須在 `stock_names.json` 白名單內**才收，擋掉亂填。
- 暱稱／理由等文字欄位**長度上限**，並做 HTML 逸出（防 XSS）。
- **同股號同方向 N 日內已存在則拒絕**，避免洗版。
- KV 總筆數上限（如 500），超過則拒收或淘汰最舊。
- **刪除需管理密碼**，只有 Ball 有；🔴 **管理密碼不得放在前端**。

### 其他紅線

- 🔴 **Worker 掛掉時，推薦區顯示「暫時無法連線」即可，不得讓整頁壞掉**
  （P2-33 血訓：盤中 Yahoo 回 null bar 導致整頁卡死）。
- UI 須明示「**送出後所有人都看得到**」。
- 是否加「推薦人暱稱」欄位＝**未定**。Ball 原始需求沒提，但沒有它就無法看誰準。建議設為選填。

---

## C. 兩者共同的注意事項

- **版面**：現況是三欄（`index.html:588-643`），手機橫滑頁籤＋桌機 `md:grid-cols-3`。
  加兩頁變五頁，**桌機版面要重新安排**。⚠ 績效回測是「表單＋結果」不是股票卡片清單，
  硬塞成第五欄會很怪——若視覺太擠，桌機可讓它獨立成一整列橫跨，手機仍走頁籤。開工時再定。
- **改 `index.html` / `app.js` 必須 bump cache-buster**（現為 `app.js?v=20260812b`，`index.html:724`）。
  這是既有硬性規則（P2-21）。
- **自選股是 localStorage 每裝置一份**（`app.js:2629`），**不能**拿來做跨使用者共享，
  也不能由伺服器寫入別人的自選。
- 開工時本檔拆成 `STOCK_PERF_SPEC.md` 與 `RECOMMEND_SPEC.md` 兩份，本檔即可刪除。
